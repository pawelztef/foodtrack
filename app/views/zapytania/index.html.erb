<p id="notice"><%= notice %></p>

<h1>Listing Zapytania</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zapytania.each do |zapytanium| %>
      <tr>
        <td><%= link_to 'Show', zapytanium %></td>
        <td><%= link_to 'Edit', edit_zapytanium_path(zapytanium) %></td>
        <td><%= link_to 'Destroy', zapytanium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Zapytanium', new_zapytanium_path %>
