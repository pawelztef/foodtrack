<p id="notice"><%= notice %></p>

<h1>Lista Tras</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tracks.each do |track| %>
      <tr>
        <td><%= link_to 'Pokaż', backend_track %></td>
        <td><%= link_to 'Edytuj', edit_backend_track_path(track) %></td>
        <td><%= link_to 'Wykasuj', backend_track, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nowa Lista', new_backend_track_path %>
